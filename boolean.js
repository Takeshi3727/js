// 年齢の設定
let age = 28;
// Calculate is child - if they are 7 or under(7歳以下＝子供)
let isChild = age <= 7;
// Calculate is senior - if they are 65 or older(65歳以上 = シニア)
let isSenior = age >= 65;
// print is child value
console.log(isChild);
// print is senior value
console.log(isSenior);
